import { PlaceholderPage } from '@/components/placeholder-page';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Configuracion"
      eyebrow="Esqueleto tecnico"
      description="Area base para futuros ajustes del ecosistema, parametros y controles de operacion."
      bullets={[
        'Preparado para crecer con categorias, reglas y auditoria.',
        'Sin CRUD ni flujos completos en esta etapa.',
      ]}
    />
  );
}

